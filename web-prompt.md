ok, the yt-sync is working well. i'd like to upgrade this from a cronjob to a full web service with a fontend. make the frontend stranger things themed. i want to host it on my k8s cluster behind the vpn at the url yt.ratermann.us make sure it uses https. you have access to kubectl cli, use it. build the app locally and test on my docker first. once it's working correctly we can deploy to k8s.
